{
    "$schema": "https://turbo.build/schema.json",
    "pipeline": {
        "build": {
            "outputs": ["dist/**"]
        },
        "type-check": {}
    }
}
// Commande pour forcer un git push 
git push origin HEAD:main
// Commande pour forcer un git push
git push origin HEAD:main --force